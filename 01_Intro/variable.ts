// string
let greeting: string = 'Hello Ganesh'

console.log(greeting);

// number
let userId: number = 334455


// boolean
let isLoggedIn: boolean = true

// when we are assigning value to variable at time of decelaration don't need to add < : type > ts smartly detects the type of it.

export {}